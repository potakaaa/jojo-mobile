{
  "riskClass": "auth or identity; permission, secret, or trust-boundary logic (CORS/trust-boundary widening)",
  "workDescription": "Section H delta ONLY: (1) extend the /staff-invite router mount in packages/api/src/index.ts with the existing adminCors object (credentialed, single-origin, byte-identical reference to the object already mounted on /api/admin and /api/auth) so apps/admin can call POST /staff-invite/start and POST /staff-invite/consume cross-origin with credentials; (2) add a magicLinkClient() plugin to apps/admin's authClient (packages/api unchanged); (3) add a new unguarded web accept route apps/admin/src/routes/staff-invite-accept.tsx that drives the existing start->verify->consume sequence. Does NOT modify the underlying /staff-invite/start or /consume route logic, token security, guard order, or the staff_invites schema -- all of that is unchanged and was already reviewed/approved under the outer plan's harness/ (Sections A-G, review-decision.json APPROVE dated 21-07-26).",
  "approver": "vc-validate-agent (automated Layer 2 delta re-validation, scoped to Section H) -- see adversarial-validation-delta.json (this folder) and the plan's '## Validate Contract Delta -- RE-VALIDATION REQUIRED (21-07-26)' -> 'Delta Validate Findings' subsection for the full analysis this classification rests on",
  "mustStopBeforeFinalize": true,
  "note": "This is a SEPARATE risk-gate entry from the outer harness/risk-gate.json, which covers Sections A-G only and must not be conflated with this delta. harness/review-decision.json (APPROVE, dated 21-07-26) covers Sections A-G ONLY -- it must NOT be read, cited, or treated as covering this Section H delta. No review-decision-delta.json exists at the time this file was written, and none was created by this VALIDATE pass. An explicit, SEPARATE human APPROVE/REJECT decision is required for Section H before its EXECUTE is authorized, matching the manual-first evidence contract applied to Sections A-G. This agent has not fabricated, inferred, or recorded any approval for this delta -- that decision is pending and must come from a real user message relayed by the orchestrator."
}
