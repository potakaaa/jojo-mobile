services:
  jojopotato-db:
    image: postgres:16-alpine
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: jojo
      POSTGRES_PASSWORD: jojo
      POSTGRES_DB: jojopotato
    volumes:
      - jojopotato-db-data:/var/lib/postgresql/data

volumes:
  jojopotato-db-data:
