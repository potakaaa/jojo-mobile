import { ComingSoon } from '@/components/coming-soon';

/** Notifications (nested Account screen). */
export default function NotificationsScreen() {
  return <ComingSoon title="Notifications" isNestedScreen />;
}
