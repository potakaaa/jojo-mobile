# Repo-wide / CI values (EAS builds, future backend services, etc).
# App-runtime config lives in apps/mobile/.env.example — see that file
# for the EXPO_PUBLIC_* variables the client bundle actually reads.

EAS_PROJECT_ID=
